# Creator Link Hub 0.1.0

Erstes Release-Manifest (pCloud Public Folder). ZIP unter `downloads/` ablegen, optional `sha256sum` in `versions.json` → `signature` eintragen (analog Rechnungsschmiede-Update).
