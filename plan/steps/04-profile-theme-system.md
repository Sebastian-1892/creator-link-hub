# Step 04 – Profile- & Theme-System

## Ziel
Profil bearbeiten, Slug (unique, formatiert), Avatar-Upload (S3/`public`), 6 Themes + JSON-Variablen für Farben.

## Checkliste
- [ ] Livewire: Profile Settings
- [ ] Slug-Validierung, reservierte Slugs (`login`, `admin`, `api`, …)
- [ ] `league/flysystem-aws-s3-v3` oder `filesystems.php` public disk für Dev
- [ ] Theme-Seeder mit CSS-Variablen in `variables`

## Abnahme
- Avatar sichtbar, Theme wechselt Public-Styles (vorbereitet in Step 06)
